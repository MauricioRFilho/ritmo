# QA do gateway público com TLS — 28/07/2026

## Veredito

**APROVAR gateway de staging em HTTPS.**

Este veredito não aprova produção nem substitui o E2E autenticado do frontend.

## DNS e transporte

- `ritmo-api.gapet.com.br` propagado e servido pela Cloudflare;
- HTTP redireciona com `301` para HTTPS;
- origem Nginx possui certificado Let's Encrypt válido:
  - CN/SAN: `ritmo-api.gapet.com.br`;
  - validade: 28/07/2026 a 26/10/2026;
- conexão HTTPS direta com a origem e através da Cloudflare validada.

## Contratos externos

- `GET https://ritmo-api.gapet.com.br/docs` → `200`;
- `GET /v1/health` → `200` e `{"status":"ok","service":"ritmo-ai"}`;
- token Bearer inválido em rota protegida → `401`;
- preflight CORS → `200`;
- `Access-Control-Allow-Origin` corresponde à URL owner-only do Sites;
- `x-request-id` presente na resposta pública.

## Regressão

`npm run check:release`: **passou**.

- build Vinext;
- 16/16 testes Node;
- 8/8 testes Python;
- 23/23 testes pgTAP;
- total: **47/47 verificações automatizadas verdes**.

## Operação

- gateway e Ollama saudáveis após 15 horas;
- worker de privacidade ativo após 15 horas;
- worker de IA ativo;
- nenhum traceback ou HTTP 5xx nos cinco minutos finais;
- limites de memória respeitados;
- Ollama continua sem porta pública;
- sudo sem senha temporário desativado.

## Próximo gate

Configurar o frontend com
`NEXT_PUBLIC_AI_GATEWAY_URL=https://ritmo-api.gapet.com.br`, publicar staging e
executar a jornada E2E autenticada. Produção permanece bloqueada pelos gates
jurídico, suporte, alertas, restore e pilotos descritos em `docs/GO-LIVE.md`.
