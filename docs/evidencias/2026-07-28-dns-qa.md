# QA após configuração do Cloudflare — 28/07/2026

## Resultado

**BLOQUEAR promoção pública até corrigir o virtual host TLS.**

## Evidências verdes

- DNS propagado e servido pela Cloudflare;
- HTTP público `GET /v1/health` → `200`, com `Server: cloudflare`;
- origem respondeu JSON do gateway e `x-request-id`;
- `npm run check:release` passou:
  - 16 testes Node;
  - 8 testes Python;
  - 23 testes pgTAP;
  - total de 47 verificações;
- build Vinext passou;
- quatro serviços da VPS permanecem provisionados.

## Falha crítica

`HTTPS GET /v1/health` respondeu `302` para `/login.html`. A Cloudflare conecta
à porta 443 da origem, mas o Nginx ainda não possui certificado/bloco TLS para
`ritmo-api.gapet.com.br`; a requisição é capturada pelo virtual host HTTPS
preexistente.

## Correção necessária

1. reativar `sudo` temporário para `codex-deploy`;
2. emitir/instalar certificado com Certbot para `ritmo-api.gapet.com.br`;
3. validar `nginx -t` e recarregar Nginx;
4. repetir health, CORS, autenticação e isolamento externo em HTTPS;
5. revogar novamente o `sudo` temporário.
