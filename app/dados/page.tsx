import "../data-page.css";
import { DataControls } from "./data-controls";

export const metadata = { title: "Seus dados — Ritmo" };

export default function DataPage() {
  return <DataControls />;
}
